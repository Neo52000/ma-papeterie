Search Shopify Admin API documentation using the Shopify AI Toolkit.

Before writing any Shopify GraphQL query or mutation, search the documentation first:

```bash
node .shopify-ai-toolkit/skills/shopify-admin/scripts/search_docs.mjs "$ARGUMENTS"
```

Use the search results to write accurate, up-to-date GraphQL operations.
Do NOT trust trained knowledge for the Shopify API — always search first.
